---
title: "ABENBDL_FIELD_PROJECTION"
description: |
  ABENBDL_FIELD_PROJECTION - ABAP Cloud language reference documentation
library: "cloud"
libraryName: "ABAP Cloud"
category: "general"
type: "abap-reference"
sourceUrl: "https://help.sap.com/doc/abapdocu_cp_index_htm/CLOUD/en-US/ABENBDL_FIELD_PROJECTION.htm"
abapFile: "ABENBDL_FIELD_PROJECTION.html"
keywords: ["select", "update", "delete", "do", "if", "case", "class", "data", "ABENBDL", "FIELD", "PROJECTION"]
---

`... field(readonly) Field1, Field2, ...;    | field(mandatory:create) Field1, Field2, ...;    | field(readonly:update) Field1, Field2, ...;    | field(suppress) Field1, Field2, ...;`

A [projection BDEF](ABENCDS_PROJ_BDEF_GLOSRY.html) automatically and implicitly inherits all the [field characteristics](ABENBDL_FIELD_CHAR.html) of its underlying projected BDEF. Field characteristics that do not require an implementation in the [ABAP behavior pool](ABENBEHAVIOR_POOL_GLOSRY.html) can be added in the projection layer under the condition that the [base BDEF](ABENRAP_BASE_BDEF_GLOSRY.html) hasn't already specified any field characteristics for the respective field.

The following field characteristics can be newly added in a projection BDEF:

The topic [RAP - `FieldCharacteristics`](ABENBDL_FIELD_CHAR.html) describes how they work.

If there is already field control defined in the underlying base BDEF, the projection BDEF cannot define other field characteristics for the same field(s). Any such attempt leads to a syntax check error.

In a managed or unmanaged RAP BO, multiple field characteristics can be combined. In a projection BDEF, there is only one valid combination: `mandatory:create, readonly:update`.

Commas can be used to classify multiple fields in the same way. Example: `field(readonly) Field1, Field2, Field3;`.

The following example demonstrates inheritance. The projection BDEF inherits managed internal numbering from its projected BDEF and therefore, the primary key field `KEY_FIELD` is automatically assigned a value.

The ABAP class `CL_DEMO_RAP_PROJECTION_NMBRNG` uses [EML](ABENEML_GLOSRY.html) to access to [RAP business object](ABENRAP_BO_GLOSRY.html) and performs the following steps:

Code Snippet:

Result: The values for the primary key field are generated automatically.

-   `readonly`
-   `mandatory:create`
-   `readonly:update`
-   `suppress`

-   Managed internal numbering, specified using the keyword [`numbering:managed`](ABENBDL_FIELD_NUMBERING.html), is inherited and it cannot be newly defined in a projection BDEF.
-   In UI scenarios, field characteristics are displayed as [RAP consumer hint](ABENRAP_CONSUMER_HINT_GLOSRY.html).
-   Field characteristics that require an implementation (currently, this is the case only for `features:instance`) cannot be added for regular fields in the projection layer, but for augmented fields. This is described in the topic [RAP - `FieldAugmentation`, Projection BDEF](ABENBDL_FIELD_AUGMENT.html).

-   It creates three entity instances by specifying a value for the field `char_field1`.
-   It commits the three new entity instances to the database.
-   Using the ABAP SQL `SELECT` statement, it displays the content of the underlying database table. It can be seen that the primary key field `KEY_FIELD` of each entity instance contains a value, which was generated by means of managed [early numbering](ABENRAP_EARLY_NUMBERING_GLOSRY.html).

projection;\\nstrict(2);\\n\\ndefine behavior for DEMO\_RAP\_PROJECTION\_NUMBERING\\n\\{\\n use create;\\n use update;\\n use delete;\\n\\n field(readonly) int\_field1;\\n\\} MODIFY ENTITIES OF DEMO\_RAP\_PROJECTION\_NUMBERING \\n ENTITY DEMO\_RAP\_PROJECTION\_NUMBERING \\n CREATE FIELDS ( char\_field1 ) WITH VALUE #( \\n ( %cid = '1' char\_field1 = 'A' ) \\n ( %cid = '2' char\_field1 = 'B' ) \\n ( %cid = '3' char\_field1 = 'C' ) ) \\n MAPPED FINAL(mapped) \\n FAILED FINAL(failed) \\n REPORTED FINAL(reported). \\n COMMIT ENTITIES. abenabap.html abenabap\_rap.html abencds\_bdef.html abenbdl.html abenbdl\_projection\_bo.html abenbdl\_define\_beh\_projection.html abenbdl\_body\_projection.html