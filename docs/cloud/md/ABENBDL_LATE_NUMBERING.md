---
title: "ABENBDL_LATE_NUMBERING"
description: |
  ABENBDL_LATE_NUMBERING - ABAP Cloud language reference documentation
library: "cloud"
libraryName: "ABAP Cloud"
category: "general"
type: "abap-reference"
sourceUrl: "https://help.sap.com/doc/abapdocu_cp_index_htm/CLOUD/en-US/ABENBDL_LATE_NUMBERING.htm"
abapFile: "ABENBDL_LATE_NUMBERING.html"
keywords: ["select", "update", "loop", "do", "if", "method", "class", "data", "ABENBDL", "LATE", "NUMBERING"]
---

\\ `...  late numbering  ...`

Defines [internal](ABENRAP_INT_NUMBERING_GLOSRY.html)\\ [late numbering](ABENRAP_LATE_NUMBERING_GLOSRY.html) for all primary key fields of a [business object entity](ABENRAP_BO_ENTITY_GLOSRY.html). Internal late numbering means:

Internal late numbering must be defined in the [entity behavior characteristics](ABENCDS_ENTITY_PROPERTIES_GLOSRY.html) for each [entity behavior definition](ABENCDS_ENTITY_BDEF_GLOSRY.html) separately. If the [parent node](ABENPARENT_NODE_GLOSRY.html) of an entity defines late numbering, then its direct [child node](ABENCHILD_NODE_GLOSRY.html) must also define late numbering.

When using `late numbering` for a draft-enabled scenario, it is mandatory that the [RAP draft table](ABENBDL_DRAFT_TABLE.html) has an additional key field `DRAFTUUID` of data type `raw(16)`.

Alternative options for numbering:

For further conceptual background on early and late numbering, see the development guide for the ABAP RESTful Application Programming Model, section [Numbering](https://help.sap.com/docs/ABAP_Cloud/f055b8bf582d4f34b91da667bc1fcce6/d85aec25222145f0b0cbbe8b02db51f0?version=sap_cross_product_abap).

The following example shows an unmanaged BDEF that defines late numbering for the root view entity `DEMO_UNMANAGED_LATE_NUMBERING`.

**Note** This example does not fully meet the requirements of the [RAP BO contract](ABENRAP_BO_CONTRACT_GLOSRY.html). It is intentionally kept short and simple and focuses on specific RAP aspects. For example, [BDEF strict mode](ABENRAP_STRICT_MODE_GLOSRY.html) is not enabled. See more information on the RAP BO contract in the [Development guide for the ABAP RESTful Application Programming Model](https://help.sap.com/docs/ABAP_Cloud/f055b8bf582d4f34b91da667bc1fcce6/3a402c5cf6a74bc1a1de080b2a7c6978?version=sap_cross_product_abap).

The ABAP behavior pool implements the local handler class `adjust_numbers`. In this class, values are defined for the primary key field `key_field`. The first new entity instance is assigned the value '1' and each new entity instance is assigned the next higher integer.

Code snippet:

You can see the complete source code in the `BP_DEMO_UNMANAGED_LATE_NUMBERICCIMP`.

Result: the values for the primary key field `KEY_FIELD` are generated automatically.

-   The [RAP runtime engine](ABENRAP_RUNTIME_ENGINE_GLOSRY.html) assigns values to the primary key fields.
-   In the [ABAP behavior pool](ABENBEHAVIOR_POOL_GLOSRY.html), the [RAP saver method](ABENABP_SAVER_METHOD_GLOSRY.html)\\ [`adjust_numbers`](ABENSAVER_ADJUST_NUMBERS.html) must be implemented to assign a final primary key value for each entity instance.
-   The key value for an instance is assigned just before the instance is saved on the database. Thus, a gapless assignment of unique keys is ensured.

-   [Managed RAP BOs](ABENMANAGED_RAP_BO_GLOSRY.html)
-   [Unmanaged RAP BOs](ABENUNMANAGED_RAP_BO_GLOSRY.html)
-   In a [projection BO](ABENRAP_PROJECTION_BO_GLOSRY.html) and in a [RAP BO interface](ABENRAP_BO_INTERFACE_GLOSRY.html), late numbering is automatically reused from the base BDEF and does not need to be explicitly defined. For details, see the topic [RAP - `EntityBehaviorCharacteristics`, Projection BDEF](ABENBDL_CHARACTER_PROJECTION.html).

-   [`early numbering`](ABENBDL_EARLY_NUMB.html)
-   [`numbering:managed`](ABENBDL_FIELD_NUMBERING.html)

-   The ABAP class `CL_DEMO_RAP_UNMANAGED_LATE_NMB` uses [EML](ABENEML_GLOSRY.html) to access the [RAP business object](ABENRAP_BO_GLOSRY.html) and performs the following steps:

-   It creates three entity instances by specifying a value for the field `data_field1_root`.
-   It commits the three new entity instances to the database.
-   Using the ABAP SQL `SELECT` statement, it displays the content of the database table. It can be seen that the primary key field `KEY_FIELD` of each entity instance contains a value, which was generated by means of late numbering.

unmanaged implementation in class bp\_demo\_unmanaged\_late\_numberi unique;\\n\\ndefine behavior for DEMO\_UNMANAGED\_LATE\_NUMBERING\\nlate numbering\\n\\{\\n create;\\n field (readonly:update) key\_field;\\n\\} METHOD adjust\_numbers. \\n DATA number TYPE i. \\n number = 1. \\n\\ \\n LOOP AT lcl\_buffer=>root\_buffer ASSIGNING FIELD-SYMBOL(<ls\_create>). \\n <ls\_create>-key\_field = number. \\n number = number + 1. \\n APPEND VALUE #( %pid = <ls\_create>-pid \\n key\_field = number ) \\n TO mapped-demo\_unmanaged\_late\_numbering. \\n ENDLOOP. \\nENDMETHOD. abenabap.html abenabap\_rap.html abencds\_bdef.html abenbdl.html abenbdl\_rap\_bo.html abenbdl\_define\_beh.html abenbdl\_character.html