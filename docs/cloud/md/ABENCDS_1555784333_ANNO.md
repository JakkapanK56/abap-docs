---
title: "Runtime"
description: |
  References the calculation class for the annotated virtual element. The query framework is left during runtime to retreive the values for the virtual element in the calculation class. To reference the calculation class, ABAP: must be added to the string reference. Kind ABAP annotation(ABENABAP
library: "cloud"
libraryName: "ABAP Cloud"
category: "general"
type: "abap-reference"
sourceUrl: "https://help.sap.com/doc/abapdocu_cp_index_htm/CLOUD/en-US/ABENCDS_1555784333_ANNO.htm"
abapFile: "ABENCDS_1555784333_ANNO.html"
keywords: ["do", "class", "data", "ABENCDS", "1555784333", "ANNO"]
---

The following table shows the [properties](ABENCDS_SAP_ANNO_PROPS.html) of the annotation:

The [annotation definition](ABENCDS_ANNO_DEFINITION_GLOSRY.html) documentation ([KTD](ABENKTD_GLOSRY.html)) is shown below.

ObjectModel annotations provide definitions of text-related aspects of the CDS-based data model.

Runtime

References the calculation class for the annotated virtual element.

The query framework is left during runtime to retreive the values for the virtual element in the calculation class.

To reference the calculation class, ABAP: must be added to the string reference.

**Kind** [ABAP annotation](ABENABAP_ANNOTATION_GLOSRY.html)\\ **Syntax** `ObjectModel.virtualElementCalculatedBy`\\ **Scope** `#ELEMENT`\\ **Type** `String(255)`\\ **Enums** `-`\\ **Default** `-`\\ **Text** `-`\\ **MDE** `-`\\ **API** `RELEASED_FOR_SAP_CLOUD_PLATFORM`