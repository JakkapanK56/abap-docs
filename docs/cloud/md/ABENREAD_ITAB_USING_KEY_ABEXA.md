---
title: "Demo for ABAP Keyword Documentation"
description: |
  n'! n'! Disclaimer:  n'! This class represents a demonstration program of the ABAP Keyword n'! Documentation, primarily intended to provide a better explanation n'! and visualization of syntax. It is not intended for production use n'! and may use demo artifacts that are not rel
library: "cloud"
libraryName: "ABAP Cloud"
category: "general"
type: "abap-reference"
sourceUrl: "https://help.sap.com/doc/abapdocu_cp_index_htm/CLOUD/en-US/ABENREAD_ITAB_USING_KEY_ABEXA.htm"
abapFile: "ABENREAD_ITAB_USING_KEY_ABEXA.html"
keywords: ["do", "while", "if", "method", "class", "data", "internal-table", "ABENREAD", "ITAB", "USING", "KEY", "ABEXA"]
---

This example measures the runtime of the statement `READ TABLE` with various table keys.

The table `itab` is a standard table with a non-unique primary key, a unique secondary sorted key, and a unique secondary hash key. For demonstration purposes, this class hides the syntax warning that different table keys in an internal table cannot contain the same components using the [pragma](ABENPRAGMA_GLOSRY.html)\\ `##TABKEY`.

The class measures the average access time of the statement `READ TABLE` to individual table lines, depending on the number of lines in the table. The lines are accessed using the different keys. The keys are specified statically in the method `measure_static` and dynamically in the method `measure_dynamic`.

The result demonstrates that the response times for static access are as expected from around 50 to 100 table lines:

In dynamic accesses, each access incurs the additional time required to evaluate the dynamically specified key.

-   When a standard table is accessed using the primary key, the average access time increases linearly with the number of table lines.
-   When a standard table is accessed using a secondary sorted key, the average access time increases logarithmically with the number of table lines.
-   When a standard table is accessed using a secondary hash key, the access time is constant.

\* Public class definition \\n"!

Demo for ABAP Keyword Documentation

\\ \\n"! \\n"!

**Disclaimer:**
\\ \\n"! This class represents a demonstration program of the ABAP Keyword \\n"! Documentation, primarily intended to provide a better explanation \\n"! and visualization of syntax. It is not intended for production use \\n"! and may use demo artifacts that are not released as APIs for use \\n"! in ABAP for Cloud Development.

\\ \\nCLASS cl\_demo\_read\_table\_using\_key DEFINITION \\n INHERITING FROM cl\_demo\_classrun \\n PUBLIC \\n CREATE PUBLIC. \\n PUBLIC SECTION. \\n METHODS main REDEFINITION. \\n\\ \\n PRIVATE SECTION. \\n DATA: m TYPE i, \\n itab TYPE STANDARD TABLE \\n OF i \\n WITH NON-UNIQUE KEY primary\_key \\n COMPONENTS table\_line \\n WITH UNIQUE SORTED KEY sorted\_key \\n COMPONENTS table\_line \\n WITH UNIQUE HASHED KEY hashed\_key \\n COMPONENTS table\_line \\n ##tabkey\[hashed\_key\]\[sorted\_key\]. \\n METHODS: measure\_static IMPORTING key TYPE string, \\n measure\_dynamic IMPORTING key TYPE string. \\nENDCLASS. \\n\\ \\n\* Public class implementation \\nCLASS cl\_demo\_read\_table\_using\_key IMPLEMENTATION. \\n METHOD main. \\n DATA: n TYPE i, \\n j TYPE i. \\n DATA max TYPE i VALUE 1000. \\n\\ \\n out->begin\_section( 'Accessing Internal Tables by Keys' ). \\n n = 10. \\n WHILE n <= max / 10. \\n IF n = 10. \\n m = 0. \\n j = 10. \\n ELSE. \\n m = n. \\n j = 9. \\n ENDIF. \\n DO j TIMES. \\n m += n. \\n itab = VALUE #( BASE itab FOR k = 1 UNTIL k > m ( k ) ). \\n out->line( ). \\n out->begin\_section( 'Static:' ). \\n measure\_static( key = \`primary\_key\` ). \\n measure\_static( key = \`sorted\_key\` ). \\n measure\_static( key = \`hashed\_key\` ). \\n out->next\_section( 'Dynamic:' ). \\n measure\_dynamic( key = \`primary\_key\` ). \\n measure\_dynamic( key = \`sorted\_key\` ). \\n measure\_dynamic( key = \`hashed\_key\` ). \\n out->end\_section( ). \\n CLEAR itab. \\n ENDDO. \\n n \*= 10. \\n ENDWHILE. \\n ENDMETHOD. \\n METHOD measure\_dynamic. \\n DATA: t1 TYPE utclong, \\n t2 TYPE utclong, \\n t TYPE decfloat34, \\n idx TYPE i. \\n CLEAR t. \\n DO m TIMES. \\n idx = sy-index. \\n t1 = utclong\_current( ). \\n READ TABLE itab \\n WITH TABLE KEY (key) \\n COMPONENTS table\_line = idx \\n TRANSPORTING NO FIELDS. \\n t2 = utclong\_current( ). \\n cl\_abap\_utclong=>diff( EXPORTING high = t2 \\n low = t1 \\n IMPORTING seconds = DATA(tdelta) ). \\n t += tdelta. \\n ENDDO. \\n t /= m. \\n out->write( |\\{ m \\} \\{ t \\}| ). \\n ENDMETHOD. \\n METHOD measure\_static. \\n DATA: t1 TYPE utclong, \\n t2 TYPE utclong, \\n t TYPE decfloat34, \\n idx TYPE i. \\n CLEAR t. \\n IF key = \`primary\_key\`. \\n DO m TIMES. \\n idx = sy-index. \\n t1 = utclong\_current( ). \\n READ TABLE itab \\n WITH TABLE KEY primary\_key \\n COMPONENTS table\_line = idx \\n TRANSPORTING NO FIELDS \\n ##primkey\[sorted\_key\] ##primkey\[hashed\_key\]. \\n t2 = utclong\_current( ). \\n cl\_abap\_utclong=>diff( EXPORTING high = t2 \\n low = t1 \\n IMPORTING seconds = DATA(tdelta) ). \\n t += tdelta. \\n ENDDO. \\n ELSEIF key = \`sorted\_key\`. \\n DO m TIMES. \\n idx = sy-index. \\n t1 = utclong\_current( ). \\n READ TABLE itab \\n WITH TABLE KEY sorted\_key \\n COMPONENTS table\_line = idx \\n TRANSPORTING NO FIELDS. \\n t2 = utclong\_current( ). \\n cl\_abap\_utclong=>diff( EXPORTING high = t2 \\n low = t1 \\n IMPORTING seconds = DATA(tdelta2) ). \\n t += tdelta2. \\n ENDDO. \\n ELSEIF key = \`hashed\_key\`. \\n DO m TIMES. \\n idx = sy-index. \\n t1 = utclong\_current( ). \\n READ TABLE itab \\n WITH TABLE KEY hashed\_key \\n COMPONENTS table\_line = idx \\n TRANSPORTING NO FIELDS. \\n t2 = utclong\_current( ). \\n cl\_abap\_utclong=>diff( EXPORTING high = t2 \\n low = t1 \\n IMPORTING seconds = DATA(tdelta3) ). \\n t += tdelta3. \\n ENDDO. \\n ENDIF. \\n t /= m. \\n out->write( |\\{ m \\} \\{ t \\}| ). \\n ENDMETHOD. \\nENDCLASS. \* Public class definition \\n"!

Demo for ABAP Keyword Documentation

\\ \\n"! \\n"!

**Disclaimer:**
\\ \\n"! This class represents a demonstration program of the ABAP Keyword \\n"! Documentation, primarily intended to provide a better explanation \\n"! and visualization of syntax. It is not intended for production use \\n"! and may use demo artifacts that are not released as APIs for use \\n"! in ABAP for Cloud Development.

\\ \\nCLASS cl\_demo\_read\_table\_using\_key DEFINITION \\n INHERITING FROM cl\_demo\_classrun \\n PUBLIC \\n CREATE PUBLIC. \\n PUBLIC SECTION. \\n METHODS main REDEFINITION. \\n\\ \\n PRIVATE SECTION. \\n DATA: m TYPE i, \\n itab TYPE STANDARD TABLE \\n OF i \\n WITH NON-UNIQUE KEY primary\_key \\n COMPONENTS table\_line \\n WITH UNIQUE SORTED KEY sorted\_key \\n COMPONENTS table\_line \\n WITH UNIQUE HASHED KEY hashed\_key \\n COMPONENTS table\_line \\n ##tabkey\[hashed\_key\]\[sorted\_key\]. \\n METHODS: measure\_static IMPORTING key TYPE string, \\n measure\_dynamic IMPORTING key TYPE string. \\nENDCLASS. \\n\\ \\n\* Public class implementation \\nCLASS cl\_demo\_read\_table\_using\_key IMPLEMENTATION. \\n METHOD main. \\n DATA: n TYPE i, \\n j TYPE i. \\n DATA max TYPE i VALUE 1000. \\n\\ \\n out->begin\_section( 'Accessing Internal Tables by Keys' ). \\n n = 10. \\n WHILE n <= max / 10. \\n IF n = 10. \\n m = 0. \\n j = 10. \\n ELSE. \\n m = n. \\n j = 9. \\n ENDIF. \\n DO j TIMES. \\n m += n. \\n itab = VALUE #( BASE itab FOR k = 1 UNTIL k > m ( k ) ). \\n out->line( ). \\n out->begin\_section( 'Static:' ). \\n measure\_static( key = \`primary\_key\` ). \\n measure\_static( key = \`sorted\_key\` ). \\n measure\_static( key = \`hashed\_key\` ). \\n out->next\_section( 'Dynamic:' ). \\n measure\_dynamic( key = \`primary\_key\` ). \\n measure\_dynamic( key = \`sorted\_key\` ). \\n measure\_dynamic( key = \`hashed\_key\` ). \\n out->end\_section( ). \\n CLEAR itab. \\n ENDDO. \\n n \*= 10. \\n ENDWHILE. \\n ENDMETHOD. \\n METHOD measure\_dynamic. \\n DATA: t1 TYPE utclong, \\n t2 TYPE utclong, \\n t TYPE decfloat34, \\n idx TYPE i. \\n CLEAR t. \\n DO m TIMES. \\n idx = sy-index. \\n t1 = utclong\_current( ). \\n READ TABLE itab \\n WITH TABLE KEY (key) \\n COMPONENTS table\_line = idx \\n TRANSPORTING NO FIELDS. \\n t2 = utclong\_current( ). \\n cl\_abap\_utclong=>diff( EXPORTING high = t2 \\n low = t1 \\n IMPORTING seconds = DATA(tdelta) ). \\n t += tdelta. \\n ENDDO. \\n t /= m. \\n out->write( |\\{ m \\} \\{ t \\}| ). \\n ENDMETHOD. \\n METHOD measure\_static. \\n DATA: t1 TYPE utclong, \\n t2 TYPE utclong, \\n t TYPE decfloat34, \\n idx TYPE i. \\n CLEAR t. \\n IF key = \`primary\_key\`. \\n DO m TIMES. \\n idx = sy-index. \\n t1 = utclong\_current( ). \\n READ TABLE itab \\n WITH TABLE KEY primary\_key \\n COMPONENTS table\_line = idx \\n TRANSPORTING NO FIELDS \\n ##primkey\[sorted\_key\] ##primkey\[hashed\_key\]. \\n t2 = utclong\_current( ). \\n cl\_abap\_utclong=>diff( EXPORTING high = t2 \\n low = t1 \\n IMPORTING seconds = DATA(tdelta) ). \\n t += tdelta. \\n ENDDO. \\n ELSEIF key = \`sorted\_key\`. \\n DO m TIMES. \\n idx = sy-index. \\n t1 = utclong\_current( ). \\n READ TABLE itab \\n WITH TABLE KEY sorted\_key \\n COMPONENTS table\_line = idx \\n TRANSPORTING NO FIELDS. \\n t2 = utclong\_current( ). \\n cl\_abap\_utclong=>diff( EXPORTING high = t2 \\n low = t1 \\n IMPORTING seconds = DATA(tdelta2) ). \\n t += tdelta2. \\n ENDDO. \\n ELSEIF key = \`hashed\_key\`. \\n DO m TIMES. \\n idx = sy-index. \\n t1 = utclong\_current( ). \\n READ TABLE itab \\n WITH TABLE KEY hashed\_key \\n COMPONENTS table\_line = idx \\n TRANSPORTING NO FIELDS. \\n t2 = utclong\_current( ). \\n cl\_abap\_utclong=>diff( EXPORTING high = t2 \\n low = t1 \\n IMPORTING seconds = DATA(tdelta3) ). \\n t += tdelta3. \\n ENDDO. \\n ENDIF. \\n t /= m. \\n out->write( |\\{ m \\} \\{ t \\}| ). \\n ENDMETHOD. \\nENDCLASS. abenabap.html abenabap\_reference.html abenabap\_data\_working.html abenitab.html abentable\_processing\_statements.html abapread\_table.html