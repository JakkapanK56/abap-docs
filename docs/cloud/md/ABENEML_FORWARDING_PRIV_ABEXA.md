---
title: "Demo for ABAP Keyword Documentation"
description: |
  n'! n'! Disclaimer:  n'! This class represents a demonstration program of the ABAP Keyword n'! Documentation, primarily intended to provide a better explanation n'! and visualization of syntax. It is not intended for production use n'! and may use demo artifacts that are not rel
library: "cloud"
libraryName: "ABAP Cloud"
category: "general"
type: "abap-reference"
sourceUrl: "https://help.sap.com/doc/abapdocu_cp_index_htm/CLOUD/en-US/ABENEML_FORWARDING_PRIV_ABEXA.htm"
abapFile: "ABENEML_FORWARDING_PRIV_ABEXA.html"
keywords: ["select", "update", "delete", "do", "if", "case", "method", "class", "data", "ABENEML", "FORWARDING", "PRIV", "ABEXA"]
---

This example demonstrates ABAP EML `MODIFY` statements executing [actions](ABENRAP_ACTION_GLOSRY.html) with the addition `FORWARDING PRIVILEGED` using a managed [RAP BO](ABENRAP_BO_GLOSRY.html). The following ABAP repository objects are used in the example:

**Data models**

**Behavior definitions**

**Behavior implementations**

The source code above uses [EML](ABENEML_GLOSRY.html) to access the RAP business object from an ABAP class. There are two ABAP EML modify requests:

-   `DEMO_MANAGED_PRIVILEGED`
-   `DEMO_MANAGED_PRIVILEGED_T`

-   `DEMO_MANAGED_PRIVILEGED`
-   `DEMO_MANAGED_PRIVILEGED_T`

-   `BP_DEMO_MANAGED_PRIVILEGED`: `BP_DEMO_MANAGED_PRIVILEGED====CCIMP`
-   `BP_DEMO_MANAGED_PRIVILEGED_T`: `BP_DEMO_MANAGED_PRIVILEGED_T==CCIMP`

-   First, a demo database table is filled with entries to work with in the example. Only the key field is filled.
-   The example includes four ABAP EML requests executing two different actions based on the created keys.
-   The first two ABAP EML requests execute the `forwarding_privileged` action. One statement executes the action with, the other without the `PRIVILEGED` addition. The implementation of the `forwarding_privileged` action in the ABAP behavior pool includes an ABAP EML request executing an action (`act`) of another RAP BO (whose root view entity is based on the same database table). This statement uses the `FORWARDING PRIVILEGED` addition, which, as the name implies, forwards the privileged access. The implementation of the `act` action includes calling the `GET_CURRENT_CONTEXT` method of the `CL_ABAP_BEHV_AUX` class, with which context information is retrieved. To visualize the (not) forwarded privileged access, fields of the RAP BO instances are updated with flags and text that indicate the (not) forwarded privileged access.
-   The next two ABAP EML requests execute the `forwarding_privileged_aux` action. One statement executes the action with, the other without the `PRIVILEGED` addition. As above, the implementation of the `forwarding_privileged_aux` action in the ABAP behavior pool includes an ABAP EML request executing an action (`act`) of another RAP BO. In this case, the method is implemented in a way that basically corresponds to the functionality of a statement that uses the `FORWARDING PRIVILEGED` addition. After all action executions, a [`COMMIT ENTITIES`](ABAPCOMMIT_ENTITIES.html) statement is executed to persist the RAP BO instances to the database. Then, the data is retrieved from the database table and output to demonstrate the effect of the implementations.

\* Public class definition \\n"!

Demo for ABAP Keyword Documentation

\\ \\n"! \\n"!

**Disclaimer:**
\\ \\n"! This class represents a demonstration program of the ABAP Keyword \\n"! Documentation, primarily intended to provide a better explanation \\n"! and visualization of syntax. It is not intended for production use \\n"! and may use demo artifacts that are not released as APIs for use \\n"! in ABAP for Cloud Development.

\\ \\nCLASS cl\_demo\_rap\_eml\_forwarding\_prv DEFINITION \\n INHERITING FROM cl\_demo\_classrun \\n PUBLIC \\n CREATE PUBLIC. \\n PUBLIC SECTION. \\n METHODS main REDEFINITION. \\n METHODS: \\n constructor, \\n fill\_dbtab. \\nENDCLASS. \\n\\ \\n\* Public class implementation \\nCLASS cl\_demo\_rap\_eml\_forwarding\_prv IMPLEMENTATION. \\n METHOD main. \\n\\ \\n out->begin\_section( \`Demonstrating the FORWARDING PRIVILEGED \` && \\n \`addition\` \\n )->line( \\n )->write\_doc( \`Executing an action that includes an EML request \` && \\n \`with FORWARDING PRIVILEGED in the implementation\` \\n )->write\_doc( \`--------- EML request without PRIVILEGED ---------\` \\n ). \\n\\ \\n MODIFY ENTITY demo\_managed\_privileged \\n EXECUTE forwarding\_privileged \\n FROM VALUE #( ( key\_field = 1 ) \\n ( key\_field = 2 ) ) \\n FAILED FINAL(f1) \\n REPORTED FINAL(r1). \\n\\ \\n COMMIT ENTITIES. \\n\\ \\n IF sy-subrc <> 0. \\n out->write\_doc( \`An issue occurred in the RAP save sequence.\` ). \\n ELSE. \\n SELECT key\_field, field1, field2 FROM demo\_tab\_root\_3 \\n WHERE key\_field BETWEEN 1 AND 2 \\n ORDER BY key\_field \\n INTO TABLE @DATA(itab). \\n\\ \\n out->write( itab ). \\n ENDIF. \\n\\ \\n out->write\_doc( \`--------- EML request with PRIVILEGED ---------\` ). \\n\\ \\n MODIFY ENTITY PRIVILEGED demo\_managed\_privileged \\n EXECUTE forwarding\_privileged \\n FROM VALUE #( ( key\_field = 3 ) \\n ( key\_field = 4 ) ) \\n FAILED FINAL(f2) \\n REPORTED FINAL(r2). \\n\\ \\n COMMIT ENTITIES. \\n\\ \\n IF sy-subrc <> 0. \\n out->write\_doc( \`An issue occurred in the RAP save sequence.\` ). \\n ELSE. \\n SELECT key\_field, field1, field2 FROM demo\_tab\_root\_3 \\n WHERE key\_field BETWEEN 3 AND 4 \\n ORDER BY key\_field \\n INTO TABLE @itab. \\n\\ \\n out->write( itab ). \\n ENDIF. \\n\\ \\n out->line( \\n )->write\_doc( \`Executing an action that includes EML requests \` && \\n \`depending on the result of the importing parameter \` && \\n \`PRIVILEGED when calling cl\_abap\_behv\_aux=>get\_current\_context\` \\n )->write\_doc( \`The FORWARDING PRIVILEGED addition is a \` && \\n \`shortcut to such an implementation.\` \\n )->write\_doc( \`--------- EML request without PRIVILEGED ---------\` \\n ). \\n\\ \\n MODIFY ENTITY demo\_managed\_privileged \\n EXECUTE forwarding\_privileged\_aux \\n FROM VALUE #( ( key\_field = 5 ) \\n ( key\_field = 6 ) ) \\n FAILED FINAL(f3) \\n REPORTED FINAL(r3). \\n\\ \\n COMMIT ENTITIES. \\n\\ \\n IF sy-subrc <> 0. \\n out->write\_doc( \`An issue occurred in the RAP save sequence.\` ). \\n ELSE. \\n SELECT key\_field, field1, field2 FROM demo\_tab\_root\_3 \\n WHERE key\_field BETWEEN 5 AND 6 \\n ORDER BY key\_field \\n INTO TABLE @itab. \\n\\ \\n out->write( itab ). \\n ENDIF. \\n\\ \\n out->write\_doc( \`--------- EML request with PRIVILEGED ---------\` ). \\n\\ \\n MODIFY ENTITY PRIVILEGED demo\_managed\_privileged \\n EXECUTE forwarding\_privileged\_aux \\n FROM VALUE #( ( key\_field = 7 ) \\n ( key\_field = 8 ) ) \\n FAILED FINAL(f4) \\n REPORTED FINAL(r4). \\n\\ \\n COMMIT ENTITIES. \\n\\ \\n IF sy-subrc <> 0. \\n out->write\_doc( \`An issue occurred in the RAP save sequence.\` ). \\n ELSE. \\n SELECT key\_field, field1, field2 FROM demo\_tab\_root\_3 \\n WHERE key\_field BETWEEN 7 AND 8 \\n ORDER BY key\_field \\n INTO TABLE @itab. \\n\\ \\n out->write( itab ). \\n ENDIF. \\n ENDMETHOD. \\n METHOD constructor. \\n super->constructor( ). \\n fill\_dbtab( ). \\n ENDMETHOD. \\n METHOD fill\_dbtab. \\n DELETE FROM demo\_tab\_root\_3. \\n MODIFY demo\_tab\_root\_3 FROM TABLE @( VALUE #( \\n ( key\_field = 1 ) \\n ( key\_field = 2 ) \\n ( key\_field = 3 ) \\n ( key\_field = 4 ) \\n ( key\_field = 5 ) \\n ( key\_field = 6 ) \\n ( key\_field = 7 ) \\n ( key\_field = 8 ) ) ). \\n ENDMETHOD. \\nENDCLASS. \* Public class definition \\n"!

Demo for ABAP Keyword Documentation

\\ \\n"! \\n"!

**Disclaimer:**
\\ \\n"! This class represents a demonstration program of the ABAP Keyword \\n"! Documentation, primarily intended to provide a better explanation \\n"! and visualization of syntax. It is not intended for production use \\n"! and may use demo artifacts that are not released as APIs for use \\n"! in ABAP for Cloud Development.

\\ \\nCLASS cl\_demo\_rap\_eml\_forwarding\_prv DEFINITION \\n INHERITING FROM cl\_demo\_classrun \\n PUBLIC \\n CREATE PUBLIC. \\n PUBLIC SECTION. \\n METHODS main REDEFINITION. \\n METHODS: \\n constructor, \\n fill\_dbtab. \\nENDCLASS. \\n\\ \\n\* Public class implementation \\nCLASS cl\_demo\_rap\_eml\_forwarding\_prv IMPLEMENTATION. \\n METHOD main. \\n\\ \\n out->begin\_section( \`Demonstrating the FORWARDING PRIVILEGED \` && \\n \`addition\` \\n )->line( \\n )->write\_doc( \`Executing an action that includes an EML request \` && \\n \`with FORWARDING PRIVILEGED in the implementation\` \\n )->write\_doc( \`--------- EML request without PRIVILEGED ---------\` \\n ). \\n\\ \\n MODIFY ENTITY demo\_managed\_privileged \\n EXECUTE forwarding\_privileged \\n FROM VALUE #( ( key\_field = 1 ) \\n ( key\_field = 2 ) ) \\n FAILED FINAL(f1) \\n REPORTED FINAL(r1). \\n\\ \\n COMMIT ENTITIES. \\n\\ \\n IF sy-subrc <> 0. \\n out->write\_doc( \`An issue occurred in the RAP save sequence.\` ). \\n ELSE. \\n SELECT key\_field, field1, field2 FROM demo\_tab\_root\_3 \\n WHERE key\_field BETWEEN 1 AND 2 \\n ORDER BY key\_field \\n INTO TABLE @DATA(itab). \\n\\ \\n out->write( itab ). \\n ENDIF. \\n\\ \\n out->write\_doc( \`--------- EML request with PRIVILEGED ---------\` ). \\n\\ \\n MODIFY ENTITY PRIVILEGED demo\_managed\_privileged \\n EXECUTE forwarding\_privileged \\n FROM VALUE #( ( key\_field = 3 ) \\n ( key\_field = 4 ) ) \\n FAILED FINAL(f2) \\n REPORTED FINAL(r2). \\n\\ \\n COMMIT ENTITIES. \\n\\ \\n IF sy-subrc <> 0. \\n out->write\_doc( \`An issue occurred in the RAP save sequence.\` ). \\n ELSE. \\n SELECT key\_field, field1, field2 FROM demo\_tab\_root\_3 \\n WHERE key\_field BETWEEN 3 AND 4 \\n ORDER BY key\_field \\n INTO TABLE @itab. \\n\\ \\n out->write( itab ). \\n ENDIF. \\n\\ \\n out->line( \\n )->write\_doc( \`Executing an action that includes EML requests \` && \\n \`depending on the result of the importing parameter \` && \\n \`PRIVILEGED when calling cl\_abap\_behv\_aux=>get\_current\_context\` \\n )->write\_doc( \`The FORWARDING PRIVILEGED addition is a \` && \\n \`shortcut to such an implementation.\` \\n )->write\_doc( \`--------- EML request without PRIVILEGED ---------\` \\n ). \\n\\ \\n MODIFY ENTITY demo\_managed\_privileged \\n EXECUTE forwarding\_privileged\_aux \\n FROM VALUE #( ( key\_field = 5 ) \\n ( key\_field = 6 ) ) \\n FAILED FINAL(f3) \\n REPORTED FINAL(r3). \\n\\ \\n COMMIT ENTITIES. \\n\\ \\n IF sy-subrc <> 0. \\n out->write\_doc( \`An issue occurred in the RAP save sequence.\` ). \\n ELSE. \\n SELECT key\_field, field1, field2 FROM demo\_tab\_root\_3 \\n WHERE key\_field BETWEEN 5 AND 6 \\n ORDER BY key\_field \\n INTO TABLE @itab. \\n\\ \\n out->write( itab ). \\n ENDIF. \\n\\ \\n out->write\_doc( \`--------- EML request with PRIVILEGED ---------\` ). \\n\\ \\n MODIFY ENTITY PRIVILEGED demo\_managed\_privileged \\n EXECUTE forwarding\_privileged\_aux \\n FROM VALUE #( ( key\_field = 7 ) \\n ( key\_field = 8 ) ) \\n FAILED FINAL(f4) \\n REPORTED FINAL(r4). \\n\\ \\n COMMIT ENTITIES. \\n\\ \\n IF sy-subrc <> 0. \\n out->write\_doc( \`An issue occurred in the RAP save sequence.\` ). \\n ELSE. \\n SELECT key\_field, field1, field2 FROM demo\_tab\_root\_3 \\n WHERE key\_field BETWEEN 7 AND 8 \\n ORDER BY key\_field \\n INTO TABLE @itab. \\n\\ \\n out->write( itab ). \\n ENDIF. \\n ENDMETHOD. \\n METHOD constructor. \\n super->constructor( ). \\n fill\_dbtab( ). \\n ENDMETHOD. \\n METHOD fill\_dbtab. \\n DELETE FROM demo\_tab\_root\_3. \\n MODIFY demo\_tab\_root\_3 FROM TABLE @( VALUE #( \\n ( key\_field = 1 ) \\n ( key\_field = 2 ) \\n ( key\_field = 3 ) \\n ( key\_field = 4 ) \\n ( key\_field = 5 ) \\n ( key\_field = 6 ) \\n ( key\_field = 7 ) \\n ( key\_field = 8 ) ) ). \\n ENDMETHOD. \\nENDCLASS. abenabap.html abenabap\_rap.html abenabap\_for\_rap\_bos.html abenabap\_consume\_rap\_bos.html abeneml.html abapcommon\_eml\_elements.html abapeml\_privileged.html