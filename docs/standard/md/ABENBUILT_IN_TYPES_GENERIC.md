---
title: "ABENBUILT_IN_TYPES_GENERIC"
description: |
  ABENBUILT_IN_TYPES_GENERIC - Standard ABAP language reference documentation
library: "standard"
libraryName: "Standard ABAP"
category: "ui"
type: "abap-reference"
sourceUrl: "https://help.sap.com/doc/abapdocu_latest_index_htm/latest/en-US/ABENBUILT_IN_TYPES_GENERIC.htm"
abapFile: "ABENBUILT_IN_TYPES_GENERIC.html"
keywords: ["if", "case", "method", "class", "data", "types", "internal-table", "field-symbol", "ABENBUILT", "TYPES", "GENERIC"]
---

The following table shows the built-in generic ABAP types. A generic data type is an incomplete type specification that comprises several complete type specifications. Apart from `object`, all generic types can be used after [`TYPE`](ABENTYPING_GENERIC.html) for the [typing](ABENTYPING_GLOSRY.html) of [field symbols](ABENFIELD_SYMBOL_GLOSRY.html) and [formal parameters](ABENFORMAL_PARAMETER_GLOSRY.html). When a data object is assigned to generically typed field symbols using the statement [`ASSIGN`](ABAPASSIGN.html), or when a data object is used as an [actual parameter](ABENACTUAL_PARAMETER_GLOSRY.html) for generically typed formal parameters in [procedure calls](ABENCALL_PROCEDURES.html), the system checks whether its concrete data type is [compatible](ABENCOMPATIBLE_GLOSRY.html) with the object, that is, whether the data type is a subset of the generic type.

The only generic types that can be used after [`TYPE REF TO`](ABAPTYPES_REFERENCES.html) are `data`, for the generic typing of data references and `object`, for the generic typing of object references.

A typing with the generic type `data` acts like a typing with the generic type `any`, with the following exception: No [numeric functions](ABENMATHEMATICAL_FUNKTION_GLOSRY.html), [description functions](ABENDESCRIPTION_FUNCTION_GLOSRY.html), or [arithmetic functions](ABENARITHMETIC_EXPRESSION_GLOSRY.html) can be passed to a formal parameter typed with the type `data`. All other expressions, built-in functions, and functional methods are possible.

In addition to the built-in generic types shown in the table above, ABAP currently includes exactly one kind of user-defined generic type. A [table type](ABENTABLE_TYPE_GLOSRY.html) defined with [`TYPES - TABLE OF`](ABAPTYPES_ITAB.html) or defined in the ABAP Dictionary is generic if the [primary table key](ABENPRIMARY_TABLE_KEY_GLOSRY.html) is not specified or specified incompletely or if the [secondary table key](ABENSECONDARY_TABLE_KEY_GLOSRY.html) of the table type is generic.

Use of the built-in generic type `numeric` to type the input parameters of a method. Parameters with any numeric data types can be passed to the method (but no other data types).

-   As a rule, generic types can be used for the [typing](ABENTYPING_GLOSRY.html) of [field symbols](ABENFIELD_SYMBOL_GLOSRY.html) and [formal parameters](ABENFORMAL_PARAMETER_GLOSRY.html) only. They cannot be used in positions, where a complete type is required, e.g. when declaring a data object. `c`, `n`, `p`, `x` and table types with generic keys are an exception to this rule. In declarative statements, the compiler implicitly uses default length, decimal places or key specification then.
-   When generic types are used for the [typing](ABENTYPING_GLOSRY.html) of [field symbols](ABENFIELD_SYMBOL_GLOSRY.html) and [formal parameters](ABENFORMAL_PARAMETER_GLOSRY.html), it must be ensured that the correct results are obtained regardless of the actual type used. For example, if `csequence` is used, it must be taken into account that the possible concrete types `c` and `string` behave differently with respect to trailing blanks, or that the concrete numeric types that are possible for `numeric` in calculations lead to different [calculation types](ABENCALCULATION_TYPE_GLOSRY.html). Especially during generalization of existing types, it may be necessary to adapt the implementation accordingly.
-   Like all generic types listed here, except `data` and `object`, the generic type `any` can only be specified directly after `TYPE`. `any` cannot be specified after [`TYPE REF TO`](ABAPTYPES_REFERENCES.html). The only generic types allowed here are `data` for fully generic data reference variables and `object` for fully generic object reference variables. Specifying `REF TO any` would define a fully generic reference variable that includes data references and object references, which is currently not possible.
-   The generic type `object` can only be specified after `REF TO` and not directly after `TYPE`.
-   The object type `object` plays a special role, since it is not actually a real generic type but the empty abstract root class of all object classes of ABAP Objects. An object reference variable typed using `REF TO object` can point to any object because of the general property of reference variables where reference variables with the static type of a superclass can point to objects in all related subclasses. When compared with a data reference variable typed using the real generic data type `data`, which can point to any data object, it is also possible to classify `object` as a generic type.
-   In the case of the generic type `p`, note that when passing functional methods or arithmetic expressions to this type of typed input parameters, the length is always set to 16. With generic types `numeric`, `simple` and `data` or `any`, this is only the case for arithmetic expressions.
-   The elementary data type `utclong` is only included in the generic types `simple`, `data`, and `any`.
-   Regardless of their [base type](ABENBASE_TYPE_GLOSRY.html), [enumerated types](ABENENUMERATED_TYPE_GLOSRY.html) are only enclosed by the generic types `any`, `data` and `simple`.

METHODS numeric\_operation IMPORTING num1 TYPE numeric \\n num2 TYPE numeric \\n RETURNING VALUE(result) TYPE decfloat34. **Type** **Description** `any` Any data type `any table` Internal table with any table type `c` Text field with a generic length `clike` Character-like (`c`, `n`, and `string`, as well as the date/time types `d`, `t` and character-like [flat structures](ABENFLAT_STRUCTURE_GLOSRY.html)) `csequence` Text-like (`c`, `string`) `data` Any data type `decfloat` [Decimal floating point number type](ABENDECFLOAT_TYPE_GLOSRY.html) (`decfloat16`, `decfloat34`) `hashed table` [Hashed table](ABENHASHED_TABLE_GLOSRY.html) `index table` [Index table](ABENINDEX_TABLE_GLOSRY.html) `n` [Numeric text](ABENNUMERIC_TEXT_GLOSRY.html) with generic length `numeric` Numeric ((`b`, `s`), `i`, `int8`, `p`, `decfloat16`, `decfloat34`, `f`) `object` Any object type (root class of the inheritance hierarchy) `p` Packed number with generic length and generic number of [decimal places](ABENFRACT_DIGIT_GLOSRY.html) `simple` Elementary data type including [enumerated types](ABENENUM_TYPE_GLOSRY.html) and structured types with exclusively character-like flat components `sorted table` [Sorted table](ABENSORTED_TABLE_GLOSRY.html) `standard table` [Standard table](ABENSTANDARD_TABLE_GLOSRY.html) `table` Standard table `x` Byte field with generic length `xsequence` Byte-like (`x`, `xstring`) abenabap.html abenabap\_reference.html abenbuilt\_in.html abenbuilt\_in\_types.html