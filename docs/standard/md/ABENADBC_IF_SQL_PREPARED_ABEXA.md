---
title: "ABENADBC_IF_SQL_PREPARED_ABEXA"
description: |
  ABENADBC_IF_SQL_PREPARED_ABEXA - Standard ABAP language reference documentation
library: "standard"
libraryName: "Standard ABAP"
category: "database"
type: "abap-reference"
sourceUrl: "https://help.sap.com/doc/abapdocu_latest_index_htm/latest/en-US/ABENADBC_IF_SQL_PREPARED_ABEXA.htm"
abapFile: "ABENADBC_IF_SQL_PREPARED_ABEXA.html"
keywords: ["select", "loop", "while", "if", "try", "catch", "method", "class", "data", "internal-table", "ABENADBC", "SQL", "PREPARED", "ABEXA"]
---

The example demonstrates how a [prepared statement](ABENCL_SQL_PREPARED_STATEMENT.html) is created and executed using ADBC.

In this example, a [query](ABENADBC_QUERY.html) is instantiated as a prepared statement and is executed with different parameters.

The work process must not switch between the calls of the prepared statement, which means that dialogs using dynpros are not possible in the corresponding loop. Instead, the parameters to be evaluated are prepared and collected in a single internal table and the results lists are prepared and collected in another internal table.

\* Public class definition \\nCLASS cl\_demo\_adbc\_if\_prepared\_stmnt DEFINITION \\n INHERITING FROM cl\_demo\_classrun \\n PUBLIC \\n CREATE PUBLIC. \\n PUBLIC SECTION. \\n METHODS main REDEFINITION. \\n\\ \\n PRIVATE SECTION. \\n DATA: BEGIN OF result\_line, \\n carrid TYPE sflight-carrid, \\n connid TYPE sflight-connid, \\n END OF result\_line, \\n result\_tab LIKE TABLE OF result\_line. \\nENDCLASS. \\n\\ \\n\* Public class implementation \\nCLASS cl\_demo\_adbc\_if\_prepared\_stmnt IMPLEMENTATION. \\n METHOD main. \\n DATA: sql TYPE REF TO cl\_sql\_prepared\_statement, \\n cols TYPE adbc\_column\_tab, \\n carrid TYPE sflight-carrid, \\n carrid\_tab TYPE TABLE OF sflight-carrid. \\n cols = VALUE #( ( CONV adbc\_name( 'CARRID' ) ) \\n ( CONV adbc\_name( 'CONNID' ) ) ). \\n carrid\_tab = VALUE #( ( CONV s\_carrid( 'AA' ) ) \\n ( CONV s\_carrid( 'LH' ) ) \\n ( CONV s\_carrid( 'UA' ) ) ). \\n\\ \\n FINAL(conn) = cl\_sql\_adbc\_connection\_builder=>default( )->get( ). \\n FINAL(builder) = conn->build\_new\_statement( ). \\n\\ \\n TRY. \\n builder->sequence( \\n |SELECT CARRID, CONNID FROM | )->table( \\n |SPFLI| )->sequence( \\n | WHERE MANDT = '\\{ sy-mandt \\}' AND CARRID = ?| ). \\n\\ \\n DATA(query) = builder->create\_prepared( )->query( ). \\n\\ \\n query->add\_input\_param\_by\_elementary( REF #( carrid ) ). \\n\\ \\n LOOP AT carrid\_tab INTO carrid. \\n FINAL(result) = query->execute\_and\_get\_result\_set( ). \\n result->set\_output\_params\_by\_struct( \\n value = REF #( result\_line ) \\n corresponding\_fields = cols ). \\n\\ \\n result\_tab = VALUE #( BASE result\_tab \\n FOR j = 1 \\n WHILE result->next\_and\_get\_rows\_processed( ) > 0 \\n ( result\_line ) ). \\n ENDLOOP. \\n conn->close\_without\_disconnect( ). \\n out->write( result\_tab ). \\n CATCH cx\_sql\_exception INTO FINAL(err). \\n out->write( err->get\_text( ) ). \\n ENDTRY. \\n ENDMETHOD. \\nENDCLASS. \* Public class definition \\nCLASS cl\_demo\_adbc\_if\_prepared\_stmnt DEFINITION \\n INHERITING FROM cl\_demo\_classrun \\n PUBLIC \\n CREATE PUBLIC. \\n PUBLIC SECTION. \\n METHODS main REDEFINITION. \\n\\ \\n PRIVATE SECTION. \\n DATA: BEGIN OF result\_line, \\n carrid TYPE sflight-carrid, \\n connid TYPE sflight-connid, \\n END OF result\_line, \\n result\_tab LIKE TABLE OF result\_line. \\nENDCLASS. \\n\\ \\n\* Public class implementation \\nCLASS cl\_demo\_adbc\_if\_prepared\_stmnt IMPLEMENTATION. \\n METHOD main. \\n DATA: sql TYPE REF TO cl\_sql\_prepared\_statement, \\n cols TYPE adbc\_column\_tab, \\n carrid TYPE sflight-carrid, \\n carrid\_tab TYPE TABLE OF sflight-carrid. \\n cols = VALUE #( ( CONV adbc\_name( 'CARRID' ) ) \\n ( CONV adbc\_name( 'CONNID' ) ) ). \\n carrid\_tab = VALUE #( ( CONV s\_carrid( 'AA' ) ) \\n ( CONV s\_carrid( 'LH' ) ) \\n ( CONV s\_carrid( 'UA' ) ) ). \\n\\ \\n FINAL(conn) = cl\_sql\_adbc\_connection\_builder=>default( )->get( ). \\n FINAL(builder) = conn->build\_new\_statement( ). \\n\\ \\n TRY. \\n builder->sequence( \\n |SELECT CARRID, CONNID FROM | )->table( \\n |SPFLI| )->sequence( \\n | WHERE MANDT = '\\{ sy-mandt \\}' AND CARRID = ?| ). \\n\\ \\n DATA(query) = builder->create\_prepared( )->query( ). \\n\\ \\n query->add\_input\_param\_by\_elementary( REF #( carrid ) ). \\n\\ \\n LOOP AT carrid\_tab INTO carrid. \\n FINAL(result) = query->execute\_and\_get\_result\_set( ). \\n result->set\_output\_params\_by\_struct( \\n value = REF #( result\_line ) \\n corresponding\_fields = cols ). \\n\\ \\n result\_tab = VALUE #( BASE result\_tab \\n FOR j = 1 \\n WHILE result->next\_and\_get\_rows\_processed( ) > 0 \\n ( result\_line ) ). \\n ENDLOOP. \\n conn->close\_without\_disconnect( ). \\n out->write( result\_tab ). \\n CATCH cx\_sql\_exception INTO FINAL(err). \\n out->write( err->get\_text( ) ). \\n ENDTRY. \\n ENDMETHOD. \\nENDCLASS. abenabap.html abenabap\_reference.html abendb\_access.html abennative\_sql.html abenadbc\_intf.html abenadbc\_if\_abexas.html