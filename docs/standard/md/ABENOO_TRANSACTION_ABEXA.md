---
title: "Execute"
description: |
  The program `DEMO_OO_TRANSACTION` is a subroutine pool that does not contain any subroutines. Instead, the program contains the definition of the local class `demo_class`, linked with the transaction code `DEMO_OO_METHOD`. When the transaction is called, the program is loaded, an instance of the cla
library: "standard"
libraryName: "Standard ABAP"
category: "general"
type: "abap-reference"
sourceUrl: "https://help.sap.com/doc/abapdocu_latest_index_htm/latest/en-US/ABENOO_TRANSACTION_ABEXA.htm"
abapFile: "ABENOO_TRANSACTION_ABEXA.html"
keywords: ["do", "method", "class", "ABENOO", "TRANSACTION", "ABEXA"]
---

This example demonstrates how a transaction code is linked with the method of a local class.

Execute

The program `DEMO_OO_TRANSACTION` is a subroutine pool that does not contain any subroutines. Instead, the program contains the definition of the local class `demo_class`, linked with the transaction code `DEMO_OO_METHOD`. When the transaction is called, the program is loaded, an instance of the class is created, and the method is called.

abenabap.html abenabap\_reference.html abenabap\_objects.html abenabap\_objects\_abexas.html