---
title: "ABENTABLE_EXP_CHAINING_ABEXA"
description: |
  ABENTABLE_EXP_CHAINING_ABEXA - Standard ABAP language reference documentation
library: "standard"
libraryName: "Standard ABAP"
category: "data-structures"
type: "abap-reference"
sourceUrl: "https://help.sap.com/doc/abapdocu_latest_index_htm/latest/en-US/ABENTABLE_EXP_CHAINING_ABEXA.htm"
abapFile: "ABENTABLE_EXP_CHAINING_ABEXA.html"
keywords: ["do", "if", "try", "catch", "method", "class", "data", "internal-table", "field-symbol", "ABENTABLE", "EXP", "CHAINING", "ABEXA"]
---

This example demonstrates how table expressions are chained.

The example shows how a nested internal table is read using the statement [`READ TABLE`](ABAPREAD_TABLE.html) and using a chained [table expression](ABENTABLE_EXPRESSIONS.html).

The result of both reads is the same.

-   Reads using statements require the statement `READ TABLE` to be used twice and two explicitly declared field symbols. The first field symbol `wa1` is used in the second `READ` statement and the second field symbol `wa2` is used to pass the result to a method.
-   When reads are performed using table expressions, they can be chained and passed to the method directly. No explicitly declared field symbols are required.

\* Public class definition \\nCLASS cl\_demo\_tab\_exp\_chaining DEFINITION \\n INHERITING FROM cl\_demo\_classrun \\n PUBLIC \\n CREATE PUBLIC. \\n PUBLIC SECTION. \\n METHODS main REDEFINITION. \\n METHODS constructor. \\n\\ \\n PRIVATE SECTION. \\n DATA: langu TYPE sy-langu, \\n index TYPE if\_abap\_docu=>abap\_index\_tab. \\nENDCLASS. \\n\\ \\n\* Public class implementation \\nCLASS cl\_demo\_tab\_exp\_chaining IMPLEMENTATION. \\n METHOD main. \\n\\ \\n "Read a column of a nested table with table statements \\n out->begin\_section( \`Table Statements\` ). \\n READ TABLE index WITH KEY key1 = 'ASSIGNING' \\n ASSIGNING FIELD-SYMBOL(). \\n READ TABLE \-docu\_objects WITH KEY key2 = 'READ TABLE itab' \\n ASSIGNING FIELD-SYMBOL(). \\n IF sy-subrc = 0. \\n FINAL(title1) = \\n cl\_abap\_docu=>get\_title( \\n langu = langu \\n object = \-docu\_name \\n version = if\_abap\_docu=>union ). \\n out->write\_data( title1 ). \\n ELSE. \\n out->write\_text( \`Nothing found\` ). \\n ENDIF. \\n\\ \\n "Read a column of a nested table with chained table expressions \\n out->next\_section( \`Table Expressions\` ). \\n TRY. \\n FINAL(title2) = \\n cl\_abap\_docu=>get\_title( \\n langu = langu \\n object = index\[ key1 = 'ASSIGNING' \\n \]-docu\_objects\[ key2 = 'READ TABLE itab' \\n \]-docu\_name \\n version = if\_abap\_docu=>union ). \\n out->write\_data( title2 ). \\n CATCH cx\_sy\_itab\_line\_not\_found ##no\_handler. \\n out->write\_text( \`Nothing found\` ). \\n ENDTRY. \\n\\ \\n ENDMETHOD. \\n METHOD constructor. \\n super->constructor( ). \\n langu = sy-langu. \\n IF langu <> 'D'. \\n langu = 'E'. \\n ENDIF. \\n FINAL(index) = cl\_abap\_docu=>get\_abap\_index( \\n langu = langu \\n version = if\_abap\_docu=>unicode ). \\n me->index = index->\*. \\n ENDMETHOD. \\nENDCLASS. \* Public class definition \\nCLASS cl\_demo\_tab\_exp\_chaining DEFINITION \\n INHERITING FROM cl\_demo\_classrun \\n PUBLIC \\n CREATE PUBLIC. \\n PUBLIC SECTION. \\n METHODS main REDEFINITION. \\n METHODS constructor. \\n\\ \\n PRIVATE SECTION. \\n DATA: langu TYPE sy-langu, \\n index TYPE if\_abap\_docu=>abap\_index\_tab. \\nENDCLASS. \\n\\ \\n\* Public class implementation \\nCLASS cl\_demo\_tab\_exp\_chaining IMPLEMENTATION. \\n METHOD main. \\n\\ \\n "Read a column of a nested table with table statements \\n out->begin\_section( \`Table Statements\` ). \\n READ TABLE index WITH KEY key1 = 'ASSIGNING' \\n ASSIGNING FIELD-SYMBOL(). \\n READ TABLE \-docu\_objects WITH KEY key2 = 'READ TABLE itab' \\n ASSIGNING FIELD-SYMBOL(). \\n IF sy-subrc = 0. \\n FINAL(title1) = \\n cl\_abap\_docu=>get\_title( \\n langu = langu \\n object = \-docu\_name \\n version = if\_abap\_docu=>union ). \\n out->write\_data( title1 ). \\n ELSE. \\n out->write\_text( \`Nothing found\` ). \\n ENDIF. \\n\\ \\n "Read a column of a nested table with chained table expressions \\n out->next\_section( \`Table Expressions\` ). \\n TRY. \\n FINAL(title2) = \\n cl\_abap\_docu=>get\_title( \\n langu = langu \\n object = index\[ key1 = 'ASSIGNING' \\n \]-docu\_objects\[ key2 = 'READ TABLE itab' \\n \]-docu\_name \\n version = if\_abap\_docu=>union ). \\n out->write\_data( title2 ). \\n CATCH cx\_sy\_itab\_line\_not\_found ##no\_handler. \\n out->write\_text( \`Nothing found\` ). \\n ENDTRY. \\n\\ \\n ENDMETHOD. \\n METHOD constructor. \\n super->constructor( ). \\n langu = sy-langu. \\n IF langu <> 'D'. \\n langu = 'E'. \\n ENDIF. \\n FINAL(index) = cl\_abap\_docu=>get\_abap\_index( \\n langu = langu \\n version = if\_abap\_docu=>unicode ). \\n me->index = index->\*. \\n ENDMETHOD. \\nENDCLASS. abenabap.html abenabap\_reference.html abenabap\_data\_working.html abenitab.html abentable\_processing\_expr\_func.html abentable\_expressions.html abentable\_exp\_chaining.html