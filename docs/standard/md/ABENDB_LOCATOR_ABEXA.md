---
title: "ABENDB_LOCATOR_ABEXA"
description: |
  ABENDB_LOCATOR_ABEXA - Standard ABAP language reference documentation
library: "standard"
libraryName: "Standard ABAP"
category: "general"
type: "abap-reference"
sourceUrl: "https://help.sap.com/doc/abapdocu_latest_index_htm/latest/en-US/ABENDB_LOCATOR_ABEXA.htm"
abapFile: "ABENDB_LOCATOR_ABEXA.html"
keywords: ["select", "loop", "if", "try", "catch", "method", "class", "data", "ABENDB", "LOCATOR", "ABEXA"]
---

This example demonstrates how to access texts in DDIC database tables using [locators](ABENLOCATOR_GLOSRY.html).

In this example, a `SELECT` loop and locators [created](ABENSELECT_INTO_LOB_HANDLES.html) there are used to access the text column of a DDIC database table in which texts from Online Text Repository (OTR) are stored in a column of the type `STRING`.

The methods of the locators make it possible to get information about the texts without them having to be transported into the ABAP class. In this example, the average length of all texts of a language is determined and the number of texts which contain a specific character string is returned.

Since the number of locators in each [database LUW](ABENDATABASE_LUW_GLOSRY.html) is limited, the locator created there must be closed explicitly at the end of each `SELECT` loop. The runtime error `DBIF_RSQL_TOO_MANY_LOB_HANDLES` occurs if the maximum number of open locators is exceeded.

\* Public class definition \\nCLASS cl\_demo\_db\_locator DEFINITION \\n INHERITING FROM cl\_demo\_classrun \\n PUBLIC \\n CREATE PUBLIC. \\n PUBLIC SECTION. \\n METHODS main REDEFINITION. \\nENDCLASS. \\n\\ \\n\* Public class implementation \\nCLASS cl\_demo\_db\_locator IMPLEMENTATION. \\n METHOD main. \\n DATA: otr\_text\_locator TYPE REF TO cl\_abap\_db\_c\_locator, \\n length TYPE i, \\n hits TYPE i, \\n avg TYPE p DECIMALS 2. \\n\\ \\n DATA: pattern TYPE string VALUE 'ABAP', \\n language TYPE sy-langu. \\n\\ \\n language = sy-langu. \\n cl\_demo\_input=>new( \\n )->add\_field( CHANGING field = pattern \\n )->request( CHANGING field = language ). \\n\\ \\n TRY. \\n SELECT text \\n FROM sotr\_textu "#EC CI\_GENBUFF \\n WHERE langu = @language \\n INTO @otr\_text\_locator. \\n\\ \\n length += otr\_text\_locator->get\_length( ). \\n\\ \\n IF otr\_text\_locator->find( start\_offset = 0 \\n pattern = pattern ) <> -1. \\n hits += 1. \\n ENDIF. \\n\\ \\n otr\_text\_locator->close( ). \\n ENDSELECT. \\n CATCH cx\_lob\_sql\_error. \\n out->write( 'Exception in locator' ). \\n RETURN. \\n ENDTRY. \\n\\ \\n avg = length / sy-dbcnt. \\n\\ \\n out->write( \\n |Average length of strings in database table: \\{ avg \\}\\\\n\\\\n| && \\n |Occurrences of "\\{ pattern WIDTH = strlen( pattern ) \\}" | && \\n |in strings of database table: \\{ hits \\}| ). \\n ENDMETHOD. \\nENDCLASS. \* Public class definition \\nCLASS cl\_demo\_db\_locator DEFINITION \\n INHERITING FROM cl\_demo\_classrun \\n PUBLIC \\n CREATE PUBLIC. \\n PUBLIC SECTION. \\n METHODS main REDEFINITION. \\nENDCLASS. \\n\\ \\n\* Public class implementation \\nCLASS cl\_demo\_db\_locator IMPLEMENTATION. \\n METHOD main. \\n DATA: otr\_text\_locator TYPE REF TO cl\_abap\_db\_c\_locator, \\n length TYPE i, \\n hits TYPE i, \\n avg TYPE p DECIMALS 2. \\n\\ \\n DATA: pattern TYPE string VALUE 'ABAP', \\n language TYPE sy-langu. \\n\\ \\n language = sy-langu. \\n cl\_demo\_input=>new( \\n )->add\_field( CHANGING field = pattern \\n )->request( CHANGING field = language ). \\n\\ \\n TRY. \\n SELECT text \\n FROM sotr\_textu "#EC CI\_GENBUFF \\n WHERE langu = @language \\n INTO @otr\_text\_locator. \\n\\ \\n length += otr\_text\_locator->get\_length( ). \\n\\ \\n IF otr\_text\_locator->find( start\_offset = 0 \\n pattern = pattern ) <> -1. \\n hits += 1. \\n ENDIF. \\n\\ \\n otr\_text\_locator->close( ). \\n ENDSELECT. \\n CATCH cx\_lob\_sql\_error. \\n out->write( 'Exception in locator' ). \\n RETURN. \\n ENDTRY. \\n\\ \\n avg = length / sy-dbcnt. \\n\\ \\n out->write( \\n |Average length of strings in database table: \\{ avg \\}\\\\n\\\\n| && \\n |Occurrences of "\\{ pattern WIDTH = strlen( pattern ) \\}" | && \\n |in strings of database table: \\{ hits \\}| ). \\n ENDMETHOD. \\nENDCLASS. abenabap.html abenabap\_reference.html abendb\_access.html abenabap\_sql.html abenstreams\_locators.html abenlobs\_abexas.html