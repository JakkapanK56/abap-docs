---
title: "No KTD found"
description: |
  No KTD found This is an optional meta-annotation for annotations annotated with `@Metadata.reference.objectType #CLAS`(DDLA:Metadata.reference.objectType). It can contain an array of ABAP interfaces. If this annotation is used, then during design time a check is executed to determine if the interf
library: "standard"
libraryName: "Standard ABAP"
category: "general"
type: "abap-reference"
sourceUrl: "https://help.sap.com/doc/abapdocu_latest_index_htm/latest/en-US/ABENCDS_218316848_ANNO.htm"
abapFile: "ABENCDS_218316848_ANNO.html"
keywords: ["do", "if", "class", "data", "ABENCDS", "218316848", "ANNO"]
---

The following table shows the [properties](ABENCDS_SAP_ANNO_PROPS.html) of the annotation:

The [annotation definition](ABENCDS_ANNO_DEFINITION_GLOSRY.html) documentation ([KTD](ABENKTD_GLOSRY.html)) is shown below.

Defines the handling of metadata of a CDS object.

For annotations of type *DevelopmentObjectRef* the object-specific attributes must be defined.

No KTD found

No KTD found

This is an optional meta-annotation for annotations annotated with [`@Metadata.reference.objectType #CLAS`](DDLA:Metadata.reference.objectType).

It can contain an array of ABAP interfaces. If this annotation is used, then during design time a check is executed to determine if the interfaces are actually implemented by the referenced ABAP class.

**Kind** [Framework-specific annotation](ABENFRMWRK_ANNOTATION_GLOSRY.html)\\ **Syntax** `Metadata.reference.class.check.implementsInterfaces[]`\\ **Scope** `#ANNOTATION`\\ **Type** `String(30)`\\ **Enums** `-`\\ **Default** `-`\\ **Text** `-`\\ **MDE** `-`\\ **API** `-` abenabap.html abencds.html abencds\_annotations.html abencds\_annotations\_sap.html abencds\_annotations\_ktd\_docu.html abencds\_frmwrk\_annos.html abencds\_metadata\_f.html